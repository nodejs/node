export declare class Comparator {
    pattern: string;
    constructor(pattern: string);
    static is(_pattern: string): boolean;
    static parse(pattern: string): Comparator;
}
export declare class Star extends Comparator {
}
export declare class Literal extends Comparator {
}
