{stream/consumers}.[Symbol.toStringTag]
{stream/consumers}.arrayBuffer
{stream/consumers}.arrayBuffer.length
{stream/consumers}.arrayBuffer.name
{stream/consumers}.blob
{stream/consumers}.blob.length
{stream/consumers}.blob.name
{stream/consumers}.buffer
{stream/consumers}.buffer.length
{stream/consumers}.buffer.name
{stream/consumers}.default
{stream/consumers}.default.arrayBuffer
{stream/consumers}.default.arrayBuffer.length
{stream/consumers}.default.arrayBuffer.name
{stream/consumers}.default.blob
{stream/consumers}.default.blob.length
{stream/consumers}.default.blob.name
{stream/consumers}.default.buffer
{stream/consumers}.default.buffer.length
{stream/consumers}.default.buffer.name
{stream/consumers}.default.json
{stream/consumers}.default.json.length
{stream/consumers}.default.json.name
{stream/consumers}.default.text
{stream/consumers}.default.text.length
{stream/consumers}.default.text.name
{stream/consumers}.json
{stream/consumers}.json.length
{stream/consumers}.json.name
{stream/consumers}.text
{stream/consumers}.text.length
{stream/consumers}.text.name
