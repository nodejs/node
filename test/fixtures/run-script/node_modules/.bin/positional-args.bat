@echo off
@shift
setlocal enabledelayedexpansion
set argv=0
set "output="
for %%x in (%*) do (
  Set /A argv+=1
  Set "arg=%%~x"
  Set "output=!output! %%~x"
)
@echo Arguments: %output%
@echo The total number of arguments are: %argv%
