import fixtures from './fixtures.js';

// eslint-disable-next-line no-restricted-syntax
const {
  fixturesDir,
  path,
  fileURL,
  readSync,
  readKey,
} = fixtures;

export {
  fixturesDir,
  path,
  fileURL,
  readSync,
  readKey,
};
