/**
 * Transform `code` into a named character reference.
 *
 * @param {number} code
 * @param {number} next
 * @param {boolean} omit
 * @param {boolean} attribute
 * @returns {string}
 */
export function toNamed(
  code: number,
  next: number,
  omit: boolean,
  attribute: boolean
): string
