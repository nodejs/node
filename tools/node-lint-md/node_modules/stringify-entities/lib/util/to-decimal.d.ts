/**
 * Transform `code` into a decimal character reference.
 *
 * @param {number} code
 * @param {number} next
 * @param {boolean} omit
 * @returns {string}
 */
export function toDecimal(code: number, next: number, omit: boolean): string
